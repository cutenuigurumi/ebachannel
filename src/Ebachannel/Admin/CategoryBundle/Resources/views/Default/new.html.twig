
<form action="{{ path('admin_category_new') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}

    <input type="submit" />
</form>

        <a href="{{ path('admin_category_index') }}">
            Back to the list
        </a>
