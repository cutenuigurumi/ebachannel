{% extends '::base.html.twig' %}

{% block title %}えばちゃんねる管理者画面{% endblock %}

{% block body %}
idを合計した数は{{ count }}です。
<h1>カテゴリ一覧</h1>
    <table>
        <tr>
        <td>ID</td>
        <td>名前</td>
        </tr>
        {% for category in category %}
        <tr>
            <td>{{ category.id }}</td>
            <td>{{ category.name }}</td>
            <td><a href="{{ path('admin_category_edit', { 'id': category.id }) }}">編集</a></td>
			<td><a href="{{ path('admin_category_delete', { 'id': category.id }) }}">削除</a></td>
        </tr>
        {% endfor %}
    </table>
<a href="{{ path('admin_category_new') }}">新規作成</a>

{% endblock %}
